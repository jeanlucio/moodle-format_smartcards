define("format_smartcards/accordion",["exports","core/ajax","theme_boost/bootstrap/collapse"],(function(_exports,_ajax,_collapse){var obj;
/**
   * Persists the accordion navstyle's manual expand/collapse choices, so a student's own
   * decision survives a page reload instead of always resetting to the "resume where you
   * left off" default content.php computes on a first-ever visit (see its
   * find_default_open_section_index()).
   *
   * The toggle click itself needs no code here: importing theme_boost/bootstrap/collapse
   * activates Bootstrap's own [data-bs-toggle="collapse"] handling for the whole page (a
   * side effect of its own module-load-time data-api registration).
   *
   * Persistence reuses core_courseformat_update_course, the exact web service and section
   * preference (course_format::get_sections_preferences(), 'contentcollapsed') the
   * standard Moodle course page already uses for its own collapsible sections — not a
   * bespoke storage mechanism — so content.php's read side and this write side agree with
   * core's own semantics.
   *
   * @module     format_smartcards/accordion
   * @copyright  2026 Jean Lúcio
   * @license    https://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
   */Object.defineProperty(_exports,"__esModule",{value:!0}),_exports.init=void 0,_ajax=(obj=_ajax)&&obj.__esModule?obj:{default:obj};const SELECTORS_CONTENT='[data-region="smartcards-content"]',SELECTORS_ACCORDION_BODY='[data-region="smartcards-accordion-body"]',handleToggle=(event,collapsed)=>{if(!event.target.matches(SELECTORS_ACCORDION_BODY))return;const content=event.target.closest(SELECTORS_CONTENT);content&&((courseid,sectionid,collapsed)=>{_ajax.default.call([{methodname:"core_courseformat_update_course",args:{action:collapsed?"section_content_collapsed":"section_content_expanded",courseid:Number(courseid),ids:[Number(sectionid)]}}])[0]})(content.dataset.courseid,event.target.dataset.sectionid,collapsed)};_exports.init=()=>{document.addEventListener("hidden.bs.collapse",(event=>handleToggle(event,!0))),document.addEventListener("shown.bs.collapse",(event=>handleToggle(event,!1)))}}));

//# sourceMappingURL=accordion.min.js.map